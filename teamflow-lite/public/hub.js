const base = new URL('.', document.baseURI).pathname.replace(/\/$/, '');
async function api(path, options = {}) { const response = await fetch(`${base}${path}`, { headers: { 'Content-Type': 'application/json' }, ...options }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || '\u8bf7\u6c42\u5931\u8d25'); return data; }
(async () => { try { const hub = await api('/api/hub'); document.querySelector('#welcome').textContent = `${hub.user.name}\uff0c\u4f60\u7684\u4e2a\u4eba AI \u5de5\u4f5c\u533a\u5df2\u7ecf\u51c6\u5907\u597d\u3002`; document.querySelector('#openStudio').onclick = () => location.href = hub.aiStudioUrl; document.querySelector('#teamHint').textContent = hub.teamKeyRequired ? '\u8bf7\u8f93\u5165\u56e2\u961f\u8d1f\u8d23\u4eba\u63d0\u4f9b\u7684 Team Key\u3002' : '\u5f53\u524d\u6ca1\u6709\u8bbe\u7f6e Team Key\uff0c\u53ef\u76f4\u63a5\u8fdb\u5165\u3002'; } catch { location.href = `${base}/`; } })();
document.querySelector('#teamAccess').addEventListener('submit', async event => { event.preventDefault(); const hint = document.querySelector('#teamHint'); try { await api('/api/hub/team-access', { method: 'POST', body: JSON.stringify({ teamKey: document.querySelector('#teamKey').value }) }); location.href = `${base}/?teamflow=1`; } catch (error) { hint.textContent = error.message; } });
document.querySelector('#logout').onclick = async () => { await api('/api/logout', { method: 'POST' }); location.href = `${base}/`; };